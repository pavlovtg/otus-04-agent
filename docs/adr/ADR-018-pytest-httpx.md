# ADR-018: pytest-httpx для мока HTTP в тестах агента

- **Контекст**: необходимо определить способ мока HTTP-запросов в тестах агента.
- **Решение**: использовать `pytest-httpx` для мока HTTP-запросов `httpx` в тестах агента.
