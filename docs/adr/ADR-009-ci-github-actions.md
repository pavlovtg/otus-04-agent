# ADR-006: CI через GitHub Actions

- **Контекст**: необходимо автоматизировать сборку, тестирование и проверку покрытия при push и PR.
- **Решение**: использовать GitHub Actions с одним job: build → test → coverage check (≥ 90%); при ошибке любого шага CI падает, PR блокируется.
